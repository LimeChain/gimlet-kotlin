# Dependabot configuration:
# https://docs.github.com/en/free-pro-team@latest/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2
updates:
  # Maintain dependencies for Gradle dependencies
  - package-ecosystem: "gradle"
    directory: "/"
    target-branch: "main"
    schedule:
      interval: "daily"
    ignore:
      # Kotlin must match the kotlin-stdlib bundled in the target IDE.
      # Newer Kotlin emits debug-metadata v2 that the older bundled
      # coroutines-debug agent rejects, crashing suspend functions on
      # Dispatchers.EDT. Bump manually in lockstep with platformVersion.
      - dependency-name: "org.jetbrains.kotlin*"
      # IntelliJ Platform Gradle Plugin — runtime API surface can shift
      # between bumps. Verify manually.
      - dependency-name: "org.jetbrains.intellij.platform*"
      # oshi runs against the IDE's bundled JNA (excluded from our
      # dependency on purpose); newer oshi may need newer JNA than the
      # IDE provides. Keep pinned to the version the target IDE ships
      # in its performanceTesting plugin; bump manually in lockstep
      # with platformVersion.
      - dependency-name: "com.github.oshi*"
  # Maintain dependencies for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "main"
    schedule:
      interval: "daily"
