# IntelliJ Platform Artifacts Repositories -> https://plugins.jetbrains.com/docs/intellij/intellij-artifacts.html

pluginGroup = com.limechain.gimlet
pluginName = Gimlet
pluginRepositoryUrl = https://github.com/LimeChain/gimlet-kotlin
# SemVer format -> https://semver.org
pluginVersion = 0.1.5

# Target IDE is RustRover. Build number ranges -> https://plugins.jetbrains.com/docs/intellij/build-number-ranges.html
# Minimum is RustRover 2026.1.1's build number; the bundled
# `intellij.rustrover.debugger.runners` content module that we declare
# in plugin.xml landed in this build.
pluginSinceBuild = 261.23567

# Upper bound for marketplace compatibility - pinned to the current
# major (261.* = RustRover 2026.1.x). Allows any minor/patch in the
# 2026.1 line, blocks 2026.2 onward until the plugin is re-verified
# and a new release is cut. Bump this in lockstep with platformVersion
# when targeting a new major.
pluginUntilBuild = 261.*

# RustRover build to download for the build sandbox (./gradlew runIde / buildPlugin).
# Latest stable per JetBrains data services at the time of this change
# (2026-05-13). Bump in lockstep with the kotlin pin if RustRover ships
# a new bundled stdlib.
platformVersion = 2026.1.1

# Plugin Dependencies -> https://plugins.jetbrains.com/docs/intellij/plugin-dependencies.html
# RustRover bundles every plugin we need directly - no marketplace deps.
# `com.intellij.nativeDebug` carries the `cidr.debugger` module
# (LLDB driver, XDebugSession integration) and `com.jetbrains.rust`
# is the same JetBrains Rust plugin that ships separately on other IDEs.
platformBundledPlugins = com.intellij.nativeDebug,com.jetbrains.rust

# Content module dep for `RsRemoteRunConfigurationType` /
# `RsCidrRunProfile` and friends (lives in
# lib/intellij.rustrover.debugger.runners.jar in the RustRover IDE core).
# Declared via bundledModules here AND <dependencies><module> in
# plugin.xml - both are required.
platformBundledModules = intellij.rustrover.debugger.runners

# Marketplace dependencies (separately distributed plugins, pinned per
# build for reproducibility). Empty today - everything we need is in
# `platformBundledPlugins` above.
platformPlugins =

# Gradle Releases -> https://github.com/gradle/gradle/releases
gradleVersion = 9.4.1

# Opt-out flag for bundling Kotlin standard library -> https://jb.gg/intellij-platform-kotlin-stdlib
kotlin.stdlib.default.dependency = false

# Enable Gradle Configuration Cache -> https://docs.gradle.org/current/userguide/configuration_cache.html
org.gradle.configuration-cache = true

# Enable Gradle Build Cache -> https://docs.gradle.org/current/userguide/build_cache.html
org.gradle.caching = true
